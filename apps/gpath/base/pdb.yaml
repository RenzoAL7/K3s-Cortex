apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: gpath
  labels:
    app.kubernetes.io/name: gpath
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gpath
