apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../growth-demo
patches:
  - target:
      kind: Ingress
      name: gpath
    patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: gpath.tech
