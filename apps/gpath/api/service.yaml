apiVersion: v1
kind: Service
metadata:
  name: gpath-api
  labels:
    app.kubernetes.io/name: gpath-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gpath-api
  ports:
    - name: http
      port: 8081
      targetPort: http
